import { MapToValuesPipePipe } from './map-to-values-pipe-pipe';

describe('MapToValuesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MapToValuesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
